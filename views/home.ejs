<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>URL Shortener</title>

    <!-- Bootstrap 5 CDN -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="icon" type="image/x-icon" href="/favicon.ico">



    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f0f1f6;
            margin: 0;
            padding: 0;
            color: #333;
        }

        /* Header */
        .navbar-brand {
            font-weight: 700;
            font-size: 1.5rem;
            color: #3b3054 !important;
        }

        .btn-signup {
            background-color: #2acfcf;
            border: none;
            border-radius: 20px;
            padding: 6px 18px;
            font-weight: 600;
            color: white;
        }

        .btn-signup:hover {
            background-color: #9be3e2;
            color: #333;
        }

        /* Hero */
        .hero {
            text-align: center;
            padding: 4rem 1rem;
        }

        .hero h1 {
            font-weight: 700;
            color: #35323e;
        }

        .hero p {
            color: #777;
            margin-bottom: 2rem;
        }

        /* Form Section */
        .shorten-box {
            background: #3b3054;
            padding: 2rem;
            border-radius: 12px;
            margin-top: -3rem;
            position: relative;
            z-index: 10;
        }

        .shorten-box input {
            border-radius: 8px;
            padding: 12px;
        }

        .shorten-box button {
            background-color: #2acfcf;
            border: none;
            border-radius: 8px;
            padding: 12px;
            font-weight: 600;
            color: white;
        }

        .shorten-box button:hover {
            background-color: #9be3e2;
            color: #333;
        }

        /* Stats Section */
        .stats {
            text-align: center;
            padding: 5rem 1rem 0rem 1rem;
        }

        .stats h2 {
            font-weight: 700;
            margin-bottom: 1rem;
            color: #35323e;
        }

        .stats p {
            color: #777;
            max-width: 600px;
            margin: 0 auto 3rem;
        }

        .card-feature {
            border: none;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
            padding: 2rem;
            text-align: left;
        }

        .card-feature h5 {
            font-weight: 600;
            margin-top: 1rem;
        }

        /* Table Styling */
        .table thead {
            background-color: #3b3054;
            color: white;
        }

        .table tbody tr:hover {
            background-color: #f1f0f7;
            transition: 0.3s;
        }

        .table a {
            text-decoration: none;
            color: #2acfcf;
            font-weight: 500;
        }

        .table a::before {
            content: "🔗 ";
        }

        .table a:hover {
            color: #9be3e2;
        }

        /* Footer */
        footer {
            background: #232127;
            color: white;
            padding: 3rem 1rem;
        }

        footer h6 {
            font-weight: 600;
            margin-bottom: 1rem;
        }

        footer a {
            color: #bfbfbf;
            text-decoration: none;
            display: block;
            margin-bottom: 0.5rem;
        }

        footer a:hover {
            color: #2acfcf;
        }

        /* 3D Shake Hover Effect for Stats Cards */
        .card-feature {
            border: none;
            border-radius: 12px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.05);
            padding: 2rem;
            text-align: left;
            background: white;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
            perspective: 1000px;
        }

        .card-feature:hover {
            animation: shake3D 0.6s ease;
            transform: rotateY(8deg) rotateX(4deg) scale(1.05);
            box-shadow: 0 10px 25px rgba(0, 0, 0, 0.15);
        }

        /* Keyframes for the 3D Shake */
        @keyframes shake3D {
            0% {
                transform: rotateY(0deg) rotateX(0deg) scale(1);
            }

            20% {
                transform: rotateY(6deg) rotateX(3deg) scale(1.03);
            }

            40% {
                transform: rotateY(-6deg) rotateX(-3deg) scale(1.03);
            }

            60% {
                transform: rotateY(4deg) rotateX(2deg) scale(1.04);
            }

            80% {
                transform: rotateY(-4deg) rotateX(-2deg) scale(1.02);
            }

            100% {
                transform: rotateY(0deg) rotateX(0deg) scale(1);
            }
        }
    </style>
</head>

<body>

    <!-- Header -->
    <nav class="navbar navbar-expand-lg bg-white shadow-sm">
        <div class="container">
            <a class="navbar-brand" href="/">Shortly</a>
            <div class="ms-auto d-flex gap-3">
                <% if (page==="login" || page==="signup" ) { %>
                    <a href="/login" class="btn btn-signup">Login</a>
                    <a href="/signup" class="btn btn-signup">Sign Up</a>
                    <% } else { %>
                        <a href="/logout" class="btn btn-signup">Logout</a>
                        <% } %>
            </div>
        </div>
    </nav>


    <!-- Hero Section -->
    <section class="hero">
        <div class="container">
            <h1>More than just shorter links</h1>
            <p>Build your brand’s recognition and get detailed insights on how your links are performing.</p>
            <button class="btn btn-signup">Get Started</button>
        </div>
    </section>

    <!-- URL Form -->
    <div class="container">
        <div class="shorten-box shadow">
            <form method="POST" action="/url" class="row g-3">
                <div class="col-md-9 col-sm-8">
                    <input type="text" name="url" class="form-control" placeholder="Shorten a link here..." required>
                </div>
                <div class="col-md-3 col-sm-4 d-grid">
                    <button type="submit">Shorten It!</button>
                </div>
            </form>
        </div>

        <!-- Generated URL -->
        <% if (locals.id) { %>
            <div class="alert alert-success text-center mt-4">
                <p class="mb-0">
                    URL generated:
                    <a href="http://localhost:8002/url/<%= id %>" target="_blank">
                        http://localhost:8002/url/<%= id %>
                    </a>
                </p>
            </div>
            <% } %>
    </div>

    <!-- Advanced Statistics Section -->
    <section class="stats">
        <div class="container">
            <h2>Advanced Statistics</h2>
            <p>Track how your links are performing across the web with our advanced statistics dashboard.</p>
        </div>
    </section>

    <!-- URL Table -->
    <div class="container pb-5">
        <% if (locals.urls) { %>
            <div class="table-responsive">
                <table class="table table-bordered table-hover align-middle text-center">
                    <thead class="table-dark">
                        <tr>
                            <th style="background-color: #3b3054;">S.No</th>
                            <th style="background-color: #3b3054;">Short ID</th>
                            <th style="background-color: #3b3054;">Redirect</th>
                            <th style="background-color: #3b3054;">Clicks</th>
                        </tr>
                    </thead>
                    <tbody>
                        <% urls.forEach((url, index)=> { %>
                            <tr>
                                <td>
                                    <%= index + 1 %>
                                </td>
                                <td>
                                    <%= url.shortID %>
                                </td>
                                <td>
                                    <a href="<%= url.redirectURL %>" target="_blank">
                                        <%= url.redirectURL %>
                                    </a>
                                </td>
                                <td>
                                    <span class="badge bg-info text-dark">
                                        <%= url.visitHistory.length %>
                                    </span>
                                </td>
                            </tr>
                            <% }) %>
                    </tbody>
                </table>
            </div>
            <% } %>
    </div>

    <!-- Stats Section -->
    <section class="stats" style="padding-bottom: 32px;">
        <div class="container">
            <div class="row g-4">
                <div class="col-md-4">
                    <div class="card-feature">
                        <h5>Brand Recognition</h5>
                        <p>Boost your brand recognition with each click. Generic links don’t mean a thing. Branded links
                            help instill
                            confidence in your content.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card-feature">
                        <h5>Detailed Records</h5>
                        <p>Gain insights into who is clicking your links. Knowing when and where people engage with your
                            content
                            helps inform better decisions.</p>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="card-feature">
                        <h5>Fully Customizable</h5>
                        <p>Improve brand awareness and content discoverability through customizable links, supercharging
                            audience
                            engagement.</p>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
    <footer>
        <div class="container">
            <div class="row">
                <div class="col-md-3 mb-4">
                    <h3>Shortly</h3>
                </div>
                <div class="col-md-3 mb-4">
                    <h6>Features</h6>
                    <a href="#">Link Shortening</a>
                    <a href="#">Branded Links</a>
                    <a href="#">Analytics</a>
                </div>
                <div class="col-md-3 mb-4">
                    <h6>Resources</h6>
                    <a href="#">Blog</a>
                    <a href="#">Developers</a>
                    <a href="#">Support</a>
                </div>
                <div class="col-md-3 mb-4">
                    <h6>Company</h6>
                    <a href="#">About</a>
                    <a href="#">Our Team</a>
                    <a href="#">Careers</a>
                    <a href="#">Contact</a>
                </div>
            </div>
        </div>
    </footer>

    <!-- Bootstrap JS Bundle -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>